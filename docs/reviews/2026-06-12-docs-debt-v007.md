---
slug: docs-debt-v007
date: 2026-06-12
reviewer: botB reviewer
scope: feat/docs-debt-v007-botB vs main（fdaf2a2 / ff41eeb / 590154c / ad94439）
issue: 131
verdict: fix-critical-then-merge
---

# docs-debt-v007 — Review (2026-06-12)

> v0.0.7 发布前终审（merge 后直接 tag + release，按发布门槛从严）。范围：`git diff main...HEAD` 4 commits + closeout 前瞻。src 只读，仅做只读核查（git log/show/merge-base、grep、两套 hook 测试复跑）。

## 核查结论总览

### DEC-1~DEC-6 符合度（全部达成）

- **DEC-1**：CLAUDE.md Layout 节逐行对照仓库实物核实——5 skills、4 agents、3 commands、2 套测试、`skills/*/references/codex-tools.md` 五个 skill 全有、根 `hooks.json` matcher `*` / `hooks/hooks.json` matcher `startup|clear|compact` 分叉表述与文件内容一致、`AGENTS.md` 确为单行指针。两个 plugin.json description 修正（Codex 侧删 `3 commands`）✅
- **DEC-2**：CONTRIBUTING「三件套」段重写为 7 目录体系；`docs/log.md` / `decision-log.md` / `docs/design-docs/roundtable.md` 死链全清；AGENTS.md 断言已软化 ✅
- **DEC-3**：INDEX.md 格式符合 lint Step 2 规范（7 目录全节、`- [slug](rel-path) — H1`、Auto-generated header）；codex-compatibility 纯 rename 移 completed/（0 内容改动）；W5 改名为全 diff 中唯一行为面 hunk ✅
- **DEC-4**：usage:92/148(FAQ)/154、roundtable:20/85/95 全部更新为 canonical relay + 双模式 hook；roundtable.md「变更记录」历史节未动 ✅
- **DEC-5**：三 manifest 全 `0.0.7`；`[0.0.7] - 2026-06-12` header + 空 `[Unreleased]` 占位；rc 历史节零删除行 ✅
- **DEC-6**：diff 未越界——skills/agents/hooks/tests 仅 W5 一处标题改名；root hooks.json `async` 未动 ✅

### tester F2/F3/F4 修复质量（ad94439，重点盯项）

- **F2**：CHANGELOG 新条目「301 assertions」与实际运行核实一致（本审复跑 `STRICT=1` → 301 passed / 0 failed / 0 known-bug）；「STRICT=1 turns known-bug reproductions into failures」与 `tests/session-start.adversarial.test.sh:45` 机制相符；攻击面列举（hostile path names / env pollution / `.roundtable.json` edge cases / workspace at scale / git anomalies）与套件头注释（敌意路径名 / .roundtable.json 畸形 / workspace 规模 / git 环境污染）相符，略去 sed-fallback 与输出契约两类属代表性概括，可接受 ✅
- **F3**：roundtable.md:100-107 示例块与 `hooks/session-start:212-222` project 模式输出**逐字段比对**：`mode: project → docs_root → docs_root_source → project_id → git_top → status` 字段名、顺序、可选性（docs_root_source 仅有来源时输出，示例取值 `walk-up` 合法）完全一致 ✅
- **F4**：`git merge-base --is-ancestor` 证实 `docs/_archive/` 删除 commit（40e05fd, #122）确在 v0.0.5 tag 祖先链内，CHANGELOG v0.0.5 Removed 节（:140,152）佐证；usage:148 / roundtable.md:139 新表述「已随 v0.0.5 清理出仓、git history 可追溯」与事实相符 ✅

### Release 完整性复核

- 版本号三处一致 `0.0.7`，全仓 `0.0.7-rc` 活性引用零残留（复跑 grep）✅
- CHANGELOG [0.0.7] 抽查重点条目（workspace mode / .roundtable.json / boundary escape / 两套测试 / CONTRIBUTING 死链 / Docs-debt sweep）与三个 PR 实际内容相符，无虚报 ✅
- 空 `[Unreleased]` 占位符合 Keep a Changelog ✅
- 两套 hook 测试本审复跑：49 passed + 301 passed（STRICT=1），行为不变性成立 ✅

## Critical

- `docs/INDEX.md:17` — 已提交的 INDEX 含 `exec-plans/active/docs-debt-v007.md` 条目，但该文件 untracked、不在 git tree（tester F1，处置已定为 closeout 收口）。**merge 前必须完成 closeout commit**，且注意顺序陷阱：closeout 同时要把 exec-plan 移 `completed/`，故终次 INDEX 重建必须发生在**移动之后**——若先入库 active/ 再重建 INDEX 再移动，发布树 INDEX 仍是断链。正确顺序见下方 closeout 核对清单 → 单 commit 收口后再 merge + tag

## Warning

- 无

## Suggestion

- `CHANGELOG.md:31`（Docs-debt sweep 条目）— ad94439 修的 stale `_archive` 表述（usage:148、roundtable:139）未在 CHANGELOG 显式出现；与 F2 同类（条目漏报）但量级更小且可视为 sweep umbrella 涵盖。如取从严口径，closeout commit 顺手在该条目尾追加一从句即可；不追加亦可发
- `docs/exec-plans/active/docs-debt-v007.md:4` — frontmatter `status: active` 在移入 `completed/` 后将成陈旧字段。现有三个 completed exec-plan 同病（先例债，本 PR 范围外），但本文件尚未入库，closeout 移动时顺手改 `status: completed` 零成本；先例三文件留待 0.0.8

## Closeout 核对清单（merge + tag 前，单 commit）

1. `mv docs/exec-plans/active/docs-debt-v007.md docs/exec-plans/completed/`（文件 untracked，直接 mv + git add；可选：frontmatter `status` 改 `completed`。frontmatter `analysis:` 相对链接 `../../reviews/...` 在 active/ 与 completed/ 深度相同，移动后仍可解析，已核）
2. （可选）CHANGELOG Docs-debt sweep 条目补 `_archive` 修复从句——若做，必须在本 commit 内、tag 前
3. 终次 INDEX 重建（在第 1 步**之后**）。相对当前 INDEX 的期望 delta，逐项核：
   - `exec-plans/active` 节清空（保留空节 header）、`exec-plans/completed` 节 +`docs-debt-v007`
   - `testing` 节 +`docs-debt-v007`（590154c 已入库但 INDEX 生成在其之前，tester F6）
   - `reviews` 节 +`2026-06-12-docs-debt-v007`（本报告）
   - `Last updated` 日期刷新；终态共 15 条目（analyze 1 / design-docs 1 / active 0 / completed 4 / testing 4 / reviews 5 / bugfixes 0）
4. 单 commit（关联 #131）后 tree-level 自检：`git ls-files docs/exec-plans/active/` 为空；`git show HEAD:docs/INDEX.md` 无任何 `exec-plans/active/` 条目；INDEX 内每条路径在 `git ls-files docs/` 中存在（工作区目检会漏，必须 tree-level）
5. merge 后 tag v0.0.7 必须打在含 closeout commit 的 merge 结果上，确保 release 树携带终态 INDEX

## Verdict

**fix-critical-then-merge** —— diff 本体质量过关（DEC 全符、F2/F3/F4 修复经独立核实无误、release 三要素齐备、行为零变更 350 assertions 全绿）；唯一 blocker 即 F1 既定的 closeout commit，按上方清单顺序执行完毕即可 merge + tag。
